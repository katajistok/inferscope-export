global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "axon"
    static_configs:
      - targets: ["localhost:9101"]
    relabel_configs:
      - target_label: cluster
        replacement: "local"
